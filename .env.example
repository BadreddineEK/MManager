# ─────────────────────────────────────────────────────────────────────────────
# Mosquée Manager — Variables d'environnement
# Copier ce fichier en .env et adapter les valeurs
# ⚠️  Ne JAMAIS commiter .env dans le repo (voir .gitignore)
# ─────────────────────────────────────────────────────────────────────────────

# ── Django ────────────────────────────────────────────────────────────────────
# Générer une clé avec : python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=salutcestmoitchoupijaidesnouveauxamisjemarcheverslecole
DJANGO_DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1,*.trycloudflare.com

# ── Base de données ────────────────────────────────────────────────────────────
POSTGRES_DB=mosque_db
POSTGRES_USER=mosque_user
POSTGRES_PASSWORD=SohCahToaAC=ABBC
# Format : postgres://USER:PASSWORD@HOST:PORT/DBNAME
# En Docker : HOST = nom du service = "db"
DATABASE_URL=postgres://mosque_user:SohCahToaAC=ABBC@db:5432/mosque_db

# ── Mosquée par défaut (onboarding) ──────────────────────────────────────────
DEFAULT_MOSQUE_SLUG=meximieux

# ── Superutilisateur initial ──────────────────────────────────────────────────
ADMIN_EMAIL=admin@mosquee.fr
ADMIN_PASSWORD=ChangeMe123!

# ── Cloudflare Tunnel (prod uniquement) ──────────────────────────────────────
# Obtenir depuis : https://one.dash.cloudflare.com → Tunnels
CLOUDFLARE_TUNNEL_TOKEN=

# ── Backups ───────────────────────────────────────────────────────────────────
BACKUP_PASSPHRASE=change-me-backup-passphrase-long-and-random
# Options : "local" ou "s3"
BACKUP_TARGET=local

# AWS S3 (uniquement si BACKUP_TARGET=s3)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=

# ── Internationalisation ──────────────────────────────────────────────────────
TIMEZONE=Europe/Paris
